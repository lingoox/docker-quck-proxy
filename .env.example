# Docker-Quck-Proxy 环境变量配置示例
# 请将此文件复制为 .env 并根据实际环境修改

# Docker Registry 上游地址（推荐 production 环境使用）
UPSTREAM=https://registry-1.docker.io

# 监听地址（可自定义端口或绑定特定接口）
LISTEN_ADDR=:5000

# 日志目录（设为空则输出到 stdout）
LOG_DIR=./logs

# Docker Compose 使用的宿主端口（可选）
HOST_PORT=5000
